"""Omok Gymnasium-style environment placeholder."""

# TODO: design board representation and game rules
# class OmokEnv:
#     def __init__(self, board_size: int = 15, render_mode: str | None = None):
#         """Initialize board state and rendering preferences."""
#
#     def reset(self, *, seed: int | None = None, options: dict | None = None):
#         """Reset the game and return initial observation/info."""
#
#     def step(self, action: int):
#         """Place a stone, update state, and return observation, reward, terminated, truncated, info."""
#
#     def render(self):
#         """Render the board in text or graphical mode (TODO)."""
#
#     def close(self):
#         """Cleanup resources."""

if __name__ == "__main__":
    print("TODO: implement Omok environment")
